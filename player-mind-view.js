(() => {
  const host=document.getElementById('playerGoals');
  if(!host)return;
  const MAX_PURSUING=3;
  const MAX_INTERESTS=12;
  let scheduled=false,observer=null;

  const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[ch]));
  function isPlayerView(){return document.body.dataset.role!=='gm'||document.body.dataset.gmPreview==='true';}
  function activeCards(){return[...host.querySelectorAll('.interest-thread:not(.interest-thread-resolved)')].filter(card=>{const kind=(card.dataset.entryKind||'').toLowerCase(),status=(card.querySelector('.interest-status')?.textContent||'').toUpperCase();return kind==='interest'&&!status.includes('DORMANT')&&!status.includes('RESOLVED');}).slice(0,MAX_INTERESTS);}
  function titleFor(card){return card.querySelector('.interest-thread-head h3')?.textContent?.trim()||'Current interest';}
  function statusFor(card){return(card.querySelector('.interest-status')?.textContent||'').toUpperCase().includes('PURSUING')?'pursuing':'interested';}
  function sourceFor(card){return card.querySelector('.interest-source strong,.interest-source-link')?.textContent?.trim()||'';}
  function sourceRouteFor(card){return card.querySelector('.interest-source-link[data-engagement-route]')?.dataset.engagementRoute||'';}
  function imageFor(title,source){const wanted=[title,source].filter(Boolean).map(v=>v.toLowerCase());for(const card of document.querySelectorAll('#currentThreadsGrid .thread-card')){const cardTitle=card.querySelector('h3')?.textContent?.trim().toLowerCase()||'';if(!wanted.some(v=>v&&(cardTitle.includes(v)||v.includes(cardTitle))))continue;const image=card.querySelector('.thread-card-image:not(.thread-card-image-fallback)');if(image?.getAttribute('src'))return image.getAttribute('src');}return'';}

  function ensureStyles(){if(document.getElementById('player-mind-view-styles'))return;const style=document.createElement('style');style.id='player-mind-view-styles';style.textContent=`
    .player-mind-view{padding:34px clamp(22px,5vw,75px) 32px;border-bottom:1px solid var(--line);background:linear-gradient(180deg,#15150f 0%,#11120e 100%)}.player-mind-view>.section-head{align-items:center;margin-bottom:12px}.player-mind-view>.section-head p{max-width:570px;font-size:11px}.player-mind-summary{margin:0 0 20px;color:#928a75;font-size:10px;line-height:1.55;border-left:2px solid #756a43;padding-left:12px}
    .mind-tier{margin-top:18px}.mind-tier-head{display:flex;justify-content:space-between;align-items:end;gap:14px;margin-bottom:9px;padding-bottom:8px;border-bottom:1px solid rgba(202,179,111,.18)}.mind-tier-head span{font-size:9px;font-weight:900;letter-spacing:.14em;color:#d7be72}.mind-tier-head strong{font:19px Georgia,serif;color:#eee3c5}.mind-tier-head small{display:block;color:#8e8774;margin-top:3px}.mind-tier-count{font-size:9px;color:#b4a574;border:1px solid rgba(202,179,111,.3);padding:5px 8px;white-space:nowrap}
    .player-mind-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px}.mind-tier-interested .player-mind-grid{grid-template-columns:repeat(4,minmax(0,1fr))}.player-mind-card{position:relative;min-height:150px;display:flex;flex-direction:column;justify-content:flex-end;isolation:isolate;border:1px solid #3a3b2e;background:#171811;overflow:hidden;box-shadow:0 12px 30px rgba(0,0,0,.18);transition:transform .2s ease,border-color .2s ease;cursor:pointer;text-align:left;color:inherit;padding:0}.mind-tier-pursuing .player-mind-card{border-color:#8c7440;min-height:175px}.player-mind-card-bg{position:absolute;inset:0;z-index:-4;width:100%;height:100%;object-fit:cover;filter:saturate(.7) contrast(1.07) brightness(.6)}.player-mind-card-fallback{position:absolute;inset:0;z-index:-4;background:radial-gradient(circle at 72% 18%,rgba(104,88,48,.55),#171811 60%)}.player-mind-card:after{content:"";position:absolute;inset:0;z-index:-2;background:linear-gradient(180deg,rgba(8,9,6,.08),rgba(8,9,6,.92) 75%,rgba(8,9,6,.99))}.player-mind-card-content{position:relative;z-index:1;padding:13px}.player-mind-card:hover{border-color:#b39750;transform:translateY(-2px)}.player-mind-topline{display:flex;justify-content:space-between;gap:8px}.player-mind-state,.player-mind-rank{font-size:7px;font-weight:900;letter-spacing:.13em;text-transform:uppercase}.player-mind-state{color:#e2c878}.player-mind-rank{color:#aaa28f}.player-mind-card h3{font:17px/1.1 Georgia,serif;margin:7px 0 6px;color:#f0e7ce;text-shadow:0 2px 10px #000}.player-mind-source{display:block;color:#a9a18e;font-size:8px;text-transform:uppercase;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.player-mind-open{display:inline-block;margin-top:7px;color:#dcc989;font-size:7px;font-weight:800;letter-spacing:.11em;text-transform:uppercase}.mind-tier-empty{border:1px dashed #484433;padding:14px;color:#8d8672;background:#15150f;font-size:11px}.mind-hierarchy-note{margin-top:18px;padding:11px 13px;background:#19170f;border:1px solid rgba(202,179,111,.18);font-size:10px;color:#9e967f}.mind-hierarchy-note b{color:#ddc77f}
    @media(max-width:1050px){.mind-tier-interested .player-mind-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:820px){.player-mind-grid,.mind-tier-interested .player-mind-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:520px){.player-mind-grid,.mind-tier-interested .player-mind-grid{grid-template-columns:1fr}.player-mind-card{min-height:140px}.player-mind-view{padding-top:28px}.mind-tier-head{align-items:start}}
  `;document.head.appendChild(style);}

  function cardMarkup(card,index,tier){const title=titleFor(card),source=sourceFor(card),id=card.dataset.goalId||'',image=imageFor(title,source),visual=image?`<img class="player-mind-card-bg" src="${esc(image)}" alt="" loading="lazy">`:'<span class="player-mind-card-fallback" aria-hidden="true"></span>',route=sourceRouteFor(card);return `<button type="button" class="player-mind-card" data-player-mind-goal="${esc(id)}">${visual}<span class="player-mind-card-content"><span class="player-mind-topline"><span class="player-mind-state">${tier==='pursuing'?'PURSUING':'INTERESTED'}</span><span class="player-mind-rank">${tier==='pursuing'?`${index+1}/${MAX_PURSUING}`:'KEPT IN VIEW'}</span></span><h3>${esc(title)}</h3>${source?`<span class="player-mind-source">${esc(source)}</span>`:''}<span class="player-mind-open">${route?'Open record →':'Open details →'}</span></span></button>`;}
  function openMind(card){const route=sourceRouteFor(card);if(route){location.hash=route;return;}window.dispatchEvent(new CustomEvent('greywake:open-player-inbox',{detail:{goalId:card.dataset.goalId||''}}));}

  function render(){observer?.disconnect();const current=host.querySelector('.player-mind-view');if(!isPlayerView()){current?.remove();observeHost();return;}const first=host.querySelector('.interest-thread,.goals-empty,.goal-form');if(!first&&/Loading|unavailable/i.test(host.textContent||'')){observeHost();return;}const active=activeCards(),pursuing=active.filter(c=>statusFor(c)==='pursuing').slice(0,MAX_PURSUING),interested=active.filter(c=>statusFor(c)==='interested');ensureStyles();const signature=JSON.stringify(active.map(c=>[c.dataset.goalId||'',titleFor(c),statusFor(c),sourceRouteFor(c)]));if(current?.dataset.mindSignature===signature){observeHost();return;}current?.remove();const section=document.createElement('section');section.className='player-mind-view';section.dataset.mindSignature=signature;section.setAttribute('aria-label',"What's on my mind");const pursuingMarkup=pursuing.length?pursuing.map((c,i)=>cardMarkup(c,i,'pursuing')).join(''):'<div class="mind-tier-empty">Nothing is being actively pursued yet. Promote an Interested item when you want the group to seriously consider it.</div>';const interestedMarkup=interested.length?interested.map((c,i)=>cardMarkup(c,i,'interested')).join(''):'<div class="mind-tier-empty">No other interests are being held in view.</div>';section.innerHTML=`<div class="section-head"><div><div class="eyebrow">YOUR PRIORITIES</div><h2>What's on my mind</h2></div><p>This is a hierarchy, not a three-item inbox. Pursuing is your active shortlist; Interested is the larger set of things your character still cares about.</p></div><p class="player-mind-summary">Questions do not use these spaces. You can keep up to ${MAX_INTERESTS} active interests, but only ${MAX_PURSUING} can be Pursuing at once.</p><section class="mind-tier mind-tier-pursuing"><div class="mind-tier-head"><div><span>TOP PRIORITY</span><strong>Pursuing</strong><small>Things you want treated as active choices for the group.</small></div><div class="mind-tier-count">${pursuing.length}/${MAX_PURSUING}</div></div><div class="player-mind-grid">${pursuingMarkup}</div></section><section class="mind-tier mind-tier-interested"><div class="mind-tier-head"><div><span>KEEP IN VIEW</span><strong>Interested</strong><small>Things that matter to your character but are not currently being pushed forward.</small></div><div class="mind-tier-count">${interested.length} INTERESTED</div></div><div class="player-mind-grid">${interestedMarkup}</div></section><div class="mind-hierarchy-note"><b>Interested → Pursuing → Group Choice.</b> Removing Pursuing drops it back to Interested; removing Interested sets it aside.</div>`;host.prepend(section);section.querySelectorAll('[data-player-mind-goal]').forEach(button=>button.addEventListener('click',()=>{const card=host.querySelector(`.interest-thread[data-goal-id="${button.dataset.playerMindGoal}"]`);if(card)openMind(card);}));observeHost();}
  function observeHost(){if(!observer)observer=new MutationObserver(schedule);observer.observe(host,{childList:true,subtree:true});}
  function schedule(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;render();});}
  observeHost();window.GreywakePlayerMindView={render:schedule};window.addEventListener('greywake:player-ready',schedule);window.addEventListener('greywake:engagement-changed',schedule);window.addEventListener('greywake:portal-live-mounted',e=>{if(e.detail?.kind==='goals')schedule();});document.addEventListener('DOMContentLoaded',schedule);schedule();
})();