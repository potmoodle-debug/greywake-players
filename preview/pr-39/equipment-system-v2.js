(() => {
  const WEAPONS={
    'longsword':{name:'Longsword',slot:'primary',burden:2,trait:'Agility',range:'Melee',damage:'d10+3',damageType:'physical',feature:'—',actions:['Longsword']},
    'battleaxe':{name:'Battleaxe',slot:'primary',burden:2,trait:'Strength',range:'Melee',damage:'d10+3',damageType:'physical',feature:'—',actions:['Battleaxe']},
    'mace':{name:'Mace',slot:'primary',burden:1,trait:'Strength',range:'Melee',damage:'d8+1',damageType:'physical',feature:'—',actions:['Mace']},
    'dagger':{name:'Dagger',slot:'primary',burden:1,trait:'Finesse',range:'Melee',damage:'d8+1',damageType:'physical',feature:'—',actions:['Dagger']},
    'quarterstaff':{name:'Quarterstaff',slot:'primary',burden:2,trait:'Instinct',range:'Melee',damage:'d10+3',damageType:'physical',feature:'—',actions:['Quarterstaff']},
    'cutlass':{name:'Cutlass',slot:'primary',burden:1,trait:'Presence',range:'Melee',damage:'d8+1',damageType:'physical',feature:'—',actions:['Cutlass']},
    'spear':{name:'Spear',slot:'primary',burden:2,trait:'Finesse',range:'Very Close',damage:'d8+3',damageType:'physical',feature:'—',actions:['Spear']},
    'shortbow':{name:'Shortbow',slot:'primary',burden:2,trait:'Agility',range:'Far',damage:'d6+3',damageType:'physical',feature:'—',actions:['Shortbow']},
    'crossbow':{name:'Crossbow',slot:'primary',burden:1,trait:'Finesse',range:'Far',damage:'d6+1',damageType:'physical',feature:'—',actions:['Crossbow']},
    'arcane-gauntlets':{name:'Arcane Gauntlets',slot:'primary',burden:2,trait:'Strength',range:'Melee',damage:'d10+3',damageType:'magic',feature:'—',spellcast:true,actions:['Arcane Gauntlets']},
    'hallowed-axe':{name:'Hallowed Axe',slot:'primary',burden:1,trait:'Strength',range:'Melee',damage:'d8+1',damageType:'magic',feature:'—',spellcast:true,actions:['Hallowed Axe']},
    'glowing-rings':{name:'Glowing Rings',slot:'primary',burden:2,trait:'Agility',range:'Very Close',damage:'d10+2',damageType:'magic',feature:'—',spellcast:true,actions:['Glowing Rings']},
    'hand-runes':{name:'Hand Runes',slot:'primary',burden:1,trait:'Instinct',range:'Very Close',damage:'d10',damageType:'magic',feature:'—',spellcast:true,actions:['Hand Runes']},
    'shortstaff':{name:'Shortstaff',slot:'primary',burden:1,trait:'Instinct',range:'Close',damage:'d8+1',damageType:'magic',feature:'—',spellcast:true,actions:['Shortstaff']},
    'dualstaff':{name:'Dualstaff',slot:'primary',burden:2,trait:'Instinct',range:'Far',damage:'d6+3',damageType:'magic',feature:'—',spellcast:true,actions:['Dualstaff']},
    'wand':{name:'Wand',slot:'primary',burden:1,trait:'Knowledge',range:'Far',damage:'d6+1',damageType:'magic',feature:'—',spellcast:true,actions:['Wand']},
    'greatstaff':{name:'Greatstaff',slot:'primary',burden:2,trait:'Knowledge',range:'Very Far',damage:'d6',damageType:'magic',feature:'Powerful: on a successful attack, roll an additional damage die and discard the lowest result.',spellcast:true,actions:['Greatstaff']},
    'round-shield':{name:'Round Shield',slot:'secondary',burden:1,trait:'Strength',range:'Melee',damage:'d4',damageType:'physical',feature:'Protective: +1 to Armor Score',actions:['Round Shield']},
    'small-dagger':{name:'Small Dagger',slot:'secondary',burden:1,trait:'Finesse',range:'Melee',damage:'d8',damageType:'physical',feature:'Paired: +2 to primary weapon damage to targets within Melee range',actions:['Small Dagger']},
    'whip':{name:'Whip',slot:'secondary',burden:1,trait:'Presence',range:'Very Close',damage:'d6',damageType:'physical',feature:'Startling: mark a Stress to force adversaries within Melee range back to Close range.',actions:['Whip','Startling']},
    'hand-crossbow':{name:'Hand Crossbow',slot:'secondary',burden:1,trait:'Finesse',range:'Far',damage:'d6+1',damageType:'physical',feature:'—',actions:['Hand Crossbow']}
  };

  const ARMORS={
    'gambeson':{name:'Gambeson Armor',score:3,meta:'Armor Score 3'},
    'leather-armor':{name:'Leather Armor',score:3,meta:'Armor Score 3'}
  };
  const CONSUMABLES={
    'minor-health':{name:'Minor Health Potion',effect:'Clear 1d4 HP',kind:'hp'},
    'minor-stamina':{name:'Minor Stamina Potion',effect:'Clear 1d4 Stress',kind:'stress'}
  };
  const CONFIG={
    marek:{name:'Marek',spellcast:true,armor:'gambeson',gear:['Torch','50 ft Rope','Basic Supplies','Small Bag of Rocks and Bones'],base:{activePrimary:'shortstaff',activeSecondary:'round-shield',inventoryWeapons:[],ownedWeapons:['shortstaff','round-shield'],consumables:{'minor-stamina':1,'minor-health':0}}},
    velmira:{name:'Velmira',spellcast:true,armor:'leather-armor',gear:['Torch','50 ft Rope','Basic Supplies','Nomadic Pack','Book being translated','Leather Satchel'],base:{activePrimary:'greatstaff',activeSecondary:null,inventoryWeapons:['whip'],ownedWeapons:['greatstaff','whip'],consumables:{'minor-stamina':1,'minor-health':0}}},
    odie:{name:'Odie',spellcast:false,armor:'gambeson',gear:['Torch','50 ft Rope','Basic Supplies','Grappling Hook','Salvage-built Prosthetic Arm','Oldwork Finger · separate and unfitted'],base:{activePrimary:'spear',activeSecondary:null,inventoryWeapons:['small-dagger'],ownedWeapons:['spear','small-dagger'],consumables:{'minor-health':1,'minor-stamina':0}}}
  };

  const PREFIX='greywake:equipment-state:v3:';
  const LEGACY_PREFIX='greywake:equipment-state:v2:';
  let activeKey=null,activeStore=null,state=null,lastConsumableResult='',lastNotice='';
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[ch]));
  const die=sides=>{if(window.crypto?.getRandomValues){const b=new Uint32Array(1);window.crypto.getRandomValues(b);return(b[0]%sides)+1;}return Math.floor(Math.random()*sides)+1;};
  const key=()=>{const k=String(window.GreywakePlayer?.character||document.body.dataset.character||'').toLowerCase();return CONFIG[k]?k:null;};
  const isPreview=()=>document.body.dataset.gmPreview==='true';
  const cfg=()=>activeKey?CONFIG[activeKey]:null;
  const storeKey=k=>`${PREFIX}${k}${isPreview()?':gmtest':''}`;
  const legacyKey=k=>`${LEGACY_PREFIX}${k}${isPreview()?':gmtest':''}`;
  const resourceAPI=()=>activeKey==='marek'?window.GreywakeResources:window.GreywakeCompanion;
  const resources=()=>resourceAPI()?.getState?.()||null;
  const weapon=id=>WEAPONS[id]||null;
  const armor=()=>ARMORS[cfg()?.armor]||null;
  const clone=value=>JSON.parse(JSON.stringify(value));

  function availableCatalog(){
    const c=cfg();if(!c)return[];
    return Object.entries(WEAPONS).filter(([,w])=>!w.spellcast||c.spellcast).map(([id,w])=>({id,...w}));
  }
  function normalise(raw){
    const c=cfg();if(!c)return null;const base=clone(c.base);
    const owned=[];[...base.ownedWeapons,...(Array.isArray(raw?.ownedWeapons)?raw.ownedWeapons:[]),raw?.activePrimary,raw?.activeSecondary,...(Array.isArray(raw?.inventoryWeapons)?raw.inventoryWeapons:[])].filter(Boolean).forEach(id=>{const w=weapon(id);if(w&&(!w.spellcast||c.spellcast)&&!owned.includes(id))owned.push(id);});
    let primary=owned.includes(raw?.activePrimary)?raw.activePrimary:base.activePrimary;
    let secondary=owned.includes(raw?.activeSecondary)?raw.activeSecondary:base.activeSecondary;
    if(primary&&weapon(primary)?.slot!=='primary')primary=base.activePrimary;
    if(secondary&&weapon(secondary)?.slot!=='secondary')secondary=null;
    if(primary&&weapon(primary)?.burden===2)secondary=null;
    const inventory=[];const source=Array.isArray(raw?.inventoryWeapons)?raw.inventoryWeapons:base.inventoryWeapons;
    [...source,...owned].forEach(id=>{if(owned.includes(id)&&id!==primary&&id!==secondary&&!inventory.includes(id)&&inventory.length<2)inventory.push(id);});
    const consumables={};Object.keys(CONSUMABLES).forEach(id=>{consumables[id]=Math.max(0,Math.min(5,Number(raw?.consumables?.[id]??base.consumables[id]??0)||0));});
    return{activePrimary:primary||null,activeSecondary:secondary||null,activeArmor:c.armor,inventoryWeapons:inventory,ownedWeapons:owned,consumables};
  }
  function migrateLegacy(k){try{const raw=JSON.parse(localStorage.getItem(legacyKey(k))||'null');return raw?{...raw,ownedWeapons:[raw.activePrimary,raw.activeSecondary,...(raw.inventoryWeapons||[]),...(CONFIG[k].base.ownedWeapons||[])].filter(Boolean)}:null;}catch(_){return null;}}
  function load(k){const sk=storeKey(k);if(activeKey===k&&activeStore===sk&&state)return state;activeKey=k;activeStore=sk;let saved=null;try{saved=JSON.parse(localStorage.getItem(sk)||'null');}catch(_){saved=null;}state=normalise(saved||migrateLegacy(k)||CONFIG[k].base);save();return state;}
  function ensureState(){const k=key();return k?load(k):null;}
  function save(){if(!state||!activeStore)return;try{localStorage.setItem(activeStore,JSON.stringify(state));}catch(_){}}
  function snapshot(){return state?{activePrimary:state.activePrimary||null,activeSecondary:state.activeSecondary||null,activeArmor:state.activeArmor||null,inventoryWeapons:[...state.inventoryWeapons],ownedWeapons:[...state.ownedWeapons],consumables:{...state.consumables}}:null;}
  function emit(reason){window.dispatchEvent(new CustomEvent('greywake:equipment-state-changed',{detail:{ok:true,key:activeKey,reason,state:snapshot()}}));}
  function commit(next,reason){if(!state)return{ok:false};state=normalise({...state,...next});save();render();emit(reason);return{ok:true,state:snapshot()};}
  function importState(remote){ensureState();if(!state||!remote)return;state=normalise(remote);save();render();}
  function setNotice(text){lastNotice=String(text||'');renderManager();renderReady();}
  function isRestOpen(){return Boolean(document.getElementById('restDialog')?.open);}
  function markStress(reason){const api=resourceAPI(),rs=resources();if(!api||!rs)return{ok:false,message:'Live Stress track unavailable.'};const free=Number(rs.maxStress)-Number(rs.stress);if(free<1)return{ok:false,message:`${cfg().name} has no free Stress slot to switch weapons under pressure.`};const result=api.markStress?.(1,{reason,cost:true});return result?.ok===false?{ok:false,message:result.message||'Could not mark Stress.'}:{ok:true};}

  function addWeapon(id){
    ensureState();const w=weapon(id);if(!state||!w||isPreview())return{ok:false,message:'Weapon cannot be added.'};if(w.spellcast&&!cfg().spellcast)return{ok:false,message:'This magic weapon requires a Spellcast trait.'};if(state.ownedWeapons.includes(id))return{ok:false,message:`${w.name} is already carried.`};if(state.inventoryWeapons.length>=2)return{ok:false,message:'Both inventory weapon slots are already full.'};
    lastNotice=`Added ${w.name} to inventory.`;return commit({ownedWeapons:[...state.ownedWeapons,id],inventoryWeapons:[...state.inventoryWeapons,id]},`Added ${w.name}`);
  }
  function removeWeapon(id){ensureState();const w=weapon(id);if(!state||!w||isPreview()||!state.inventoryWeapons.includes(id))return{ok:false};const protectedBase=cfg().base.ownedWeapons.includes(id);if(protectedBase)return{ok:false,message:'Starting equipment is part of the canonical character record.'};lastNotice=`Removed ${w.name} from carried inventory.`;return commit({ownedWeapons:state.ownedWeapons.filter(x=>x!==id),inventoryWeapons:state.inventoryWeapons.filter(x=>x!==id)},`Removed ${w.name}`);}
  function equip(id,{danger=false}={}){
    ensureState();if(!state||!state.inventoryWeapons.includes(id))return{ok:false};const w=weapon(id);if(!w)return{ok:false};if(danger&&!isRestOpen()){const paid=markStress(`Switch weapon · ${w.name}`);if(!paid.ok){setNotice(paid.message);return paid;}}
    let primary=state.activePrimary,secondary=state.activeSecondary,inventory=state.inventoryWeapons.filter(x=>x!==id);
    if(w.slot==='primary'){if(primary&&!inventory.includes(primary))inventory.push(primary);primary=id;if(w.burden===2&&secondary){if(!inventory.includes(secondary))inventory.push(secondary);secondary=null;}}
    else{if(secondary&&!inventory.includes(secondary))inventory.push(secondary);secondary=id;if(primary&&weapon(primary)?.burden===2){if(!inventory.includes(primary))inventory.push(primary);primary=null;}}
    if(inventory.length>2){setNotice('That switch would exceed the two inventory-weapon slots.');return{ok:false,message:lastNotice};}
    lastNotice=`Equipped ${w.name}${danger&&!isRestOpen()?' · marked 1 Stress':''}.`;const result=commit({activePrimary:primary,activeSecondary:secondary,inventoryWeapons:inventory},`Equipped ${w.name}`);closeDialog();return result;
  }

  function ensureDialog(){let d=document.getElementById('equipmentDialog');if(!d){d=document.createElement('dialog');d.id='equipmentDialog';d.className='equipment-dialog';(document.getElementById('characterPageView')||document.body).appendChild(d);d.addEventListener('click',event=>{if(event.target===d)d.close();});}return d;}
  function closeDialog(){const d=document.getElementById('equipmentDialog');if(d?.open)d.close();}
  function openEquip(id){ensureState();const w=weapon(id);if(!w||!state?.inventoryWeapons.includes(id))return;if(isRestOpen()){equip(id,{danger:false});return;}const d=ensureDialog();d.innerHTML=`<div class="equip-dialog-shell"><div class="equip-dialog-head"><div><span>SWITCH WEAPON</span><h2>Equip ${esc(w.name)}</h2><p>Choose the fictional situation. The sheet enforces burden and inventory limits automatically.</p></div><button class="equip-dialog-close" type="button" data-close aria-label="Close">×</button></div><div class="equip-contexts"><div class="equip-context"><strong>Calm / preparing</strong><p>Equip freely when there is time to prepare, including during a rest.</p><button type="button" data-context="calm">Equip free</button></div><div class="equip-context"><strong>Danger / under pressure</strong><p>Mark 1 Stress, then switch to the inventory weapon.</p><button type="button" data-context="danger">Mark 1 Stress & equip</button></div></div></div>`;d.querySelector('[data-close]')?.addEventListener('click',closeDialog);d.querySelector('[data-context="calm"]')?.addEventListener('click',()=>equip(id,{danger:false}));d.querySelector('[data-context="danger"]')?.addEventListener('click',()=>equip(id,{danger:true}));if(typeof d.showModal==='function'&&!d.open)d.showModal();else d.setAttribute('open','');}

  function useConsumable(id){ensureState();const item=CONSUMABLES[id],count=state?.consumables?.[id]||0,api=resourceAPI(),rs=resources();if(!item||count<1||!api||!rs)return;const current=Number(rs[item.kind]||0);if(current<=0){setNotice(`${cfg().name} has no marked ${item.kind==='hp'?'HP':'Stress'} to clear.`);return;}const roll=die(4),cleared=Math.min(roll,current);api.setResource?.(item.kind,current-cleared,`${item.name} · clear ${roll}`);lastConsumableResult=`${item.name}: rolled ${roll}; cleared ${cleared} ${item.kind==='hp'?'HP':'Stress'}.`;lastNotice='';commit({consumables:{...state.consumables,[id]:count-1}},`Used ${item.name}`);}
  function adjustConsumable(id,delta){ensureState();if(!CONSUMABLES[id]||!state)return{ok:false};const count=Math.max(0,Math.min(5,(state.consumables[id]||0)+Number(delta||0)));return commit({consumables:{...state.consumables,[id]:count}},`${delta>0?'Add':'Remove'} ${CONSUMABLES[id].name}`);}

  const readNumber=text=>{const m=String(text||'').match(/[+−-]?\d+/);return m?Number(m[0].replace('−','-')):0;};
  function traitValue(name){const card=[...document.querySelectorAll('#characterSheet .sheet-grid.traits .sheet-card')].find(c=>c.querySelector('h4')?.textContent.trim()===name);return readNumber(card?.querySelector('.sheet-value')?.textContent);}
  function proficiency(){const stat=[...document.querySelectorAll('#characterSheet .character-stat')].find(n=>n.querySelector('span')?.textContent.trim().toLowerCase()==='proficiency');const n=readNumber(stat?.querySelector('strong')?.textContent);return Math.max(1,n||1);}
  function parseDamage(text){const m=String(text||'').match(/d(\d+)([+-]\d+)?/i);return m?{sides:Number(m[1]),mod:Number(m[2]||0)}:null;}
  function openWeaponUse(id){ensureState();const w=weapon(id);if(!w||!(state.activePrimary===id||state.activeSecondary===id))return;const d=ensureDialog(),trait=traitValue(w.trait),prof=proficiency();d.innerHTML=`<div class="equip-dialog-shell"><div class="equip-dialog-head"><div><span>ACTIVE WEAPON</span><h2>${esc(w.name)}</h2><p>${esc(w.trait)} ${trait>=0?'+':''}${trait} · ${esc(w.range)} · ${prof}${esc(w.damage)} ${esc(w.damageType)}</p></div><button class="equip-dialog-close" type="button" data-close aria-label="Close">×</button></div><div class="equip-contexts"><div class="equip-context" style="grid-column:1/-1"><strong>${esc(w.feature||'—')}</strong><p>Roll the Duality Dice for an attack. Enter Difficulty if the GM gives one.</p><label>Difficulty <input type="number" min="1" max="40" data-weapon-difficulty style="width:90px;margin-left:8px"></label><button type="button" data-roll-weapon>Roll Attack</button><div data-weapon-result style="margin-top:12px"></div></div></div></div>`;d.querySelector('[data-close]')?.addEventListener('click',closeDialog);d.querySelector('[data-roll-weapon]')?.addEventListener('click',()=>rollWeapon(id,d));if(typeof d.showModal==='function'&&!d.open)d.showModal();else d.setAttribute('open','');}
  function rollWeapon(id,d){const w=weapon(id),api=resourceAPI();if(!w)return;const hope=die(12),fear=die(12),crit=hope===fear,axis=crit||hope>fear?'Hope':'Fear',trait=traitValue(w.trait),total=hope+fear+trait,diff=Number(d.querySelector('[data-weapon-difficulty]')?.value||0),success=crit||!diff||total>=diff;if(api){if(crit){api.gainHope?.(1,'Critical success');api.clearStress?.(1,'Critical success');}else if(axis==='Hope')api.gainHope?.(1,'Roll with Hope');}const dmg=parseDamage(w.damage),prof=proficiency();let damage='';if(success&&dmg){const rolls=Array.from({length:prof},()=>die(dmg.sides)),base=rolls.reduce((a,b)=>a+b,0)+dmg.mod,bonus=crit?prof*dmg.sides:0;damage=` · Damage ${base+bonus} ${w.damageType}${crit?' (critical)':''}`;}const headline=crit?'CRITICAL SUCCESS':diff?(success?'SUCCESS':'FAILURE'):`${total} WITH ${axis.toUpperCase()}`;d.querySelector('[data-weapon-result]').innerHTML=`<b>${headline}</b><br>Hope ${hope} · Fear ${fear} · ${esc(w.trait)} ${trait>=0?'+':''}${trait} · Total ${total}${diff?` / ${diff}`:''}${damage}<br><small>${crit?'Gain 1 Hope and clear 1 Stress':axis==='Hope'?'Gain 1 Hope':'GM gains 1 Fear'}</small>`;}

  function groupByTitle(title){return[...document.querySelectorAll('#characterSheet .sheet-group')].find(g=>g.querySelector('.sheet-group-head h3')?.textContent.trim()===title)||null;}
  function ensureManager(){const group=groupByTitle('Weapons, armor & inventory');if(!group)return null;let manager=document.getElementById('equipmentManager');if(!manager){manager=document.createElement('section');manager.id='equipmentManager';manager.className='equipment-manager live-equipment-manager';const head=group.querySelector('.sheet-group-head');if(head)head.insertAdjacentElement('afterend',manager);else group.prepend(manager);}return manager;}
  function ensureReady(){const dashboard=document.querySelector('#playDashboard .play-dashboard-content');if(!dashboard)return null;let panel=document.getElementById('readyGearPanel');if(!panel){panel=document.createElement('section');panel.id='readyGearPanel';panel.className='ready-gear-panel';dashboard.appendChild(panel);}return panel;}
  function weaponMeta(w){return `${w.trait} · ${w.range} · ${w.damage} ${w.damageType}${w.feature&&w.feature!=='—'?` · ${w.feature}`:''}`;}
  function activeMarkup(id,label){const w=weapon(id);if(!w)return`<div class="equip-item active empty"><span>${esc(label)}</span><strong>Empty</strong><em>No weapon equipped.</em></div>`;return`<div class="equip-item active"><span>${esc(label)}</span><strong>${esc(w.name)}</strong><p>${esc(weaponMeta(w))}</p><em>${w.burden===2?'Two-Handed':'One-Handed'}</em><div class="equip-item-actions"><button type="button" data-use-weapon="${id}">Use</button></div></div>`;}
  function inventoryMarkup(id){const w=weapon(id);if(!w)return'';const removable=!cfg().base.ownedWeapons.includes(id);return`<div class="equip-item inventory"><span>INVENTORY WEAPON</span><strong>${esc(w.name)}</strong><p>${esc(weaponMeta(w))}</p><em>${w.burden===2?'Two-Handed':'One-Handed'} · ${esc(w.slot)}</em><div class="equip-item-actions"><button type="button" data-equip="${id}">Equip</button>${removable?`<button type="button" data-remove-weapon="${id}">Remove</button>`:''}</div></div>`;}
  function consumableMarkup(id){const item=CONSUMABLES[id],count=state.consumables[id]||0;if(count<=0)return'';const rs=resources(),canUse=Number(rs?.[item.kind]||0)>0;return`<div class="equip-item consumable"><span>CONSUMABLE</span><strong>${esc(item.name)}</strong><p>${esc(item.effect)} · one use.</p><div class="equip-item-actions"><button type="button" data-consume="${id}" ${canUse?'':'disabled'}>Use</button><button type="button" data-consume-adjust="${id}" data-delta="-1">−</button><b class="consumable-count">${count}</b><button type="button" data-consume-adjust="${id}" data-delta="1" ${count>=5?'disabled':''}>+</button></div></div>`;}
  function renderManager(){if(!state)return;const manager=ensureManager();if(!manager)return;const inventory=state.inventoryWeapons.map(inventoryMarkup).join('');const consumables=Object.keys(CONSUMABLES).map(consumableMarkup).join('');const gear=cfg().gear.map(item=>`<div class="equip-item inventory gear-only"><span>GEAR</span><strong>${esc(item)}</strong></div>`).join('');const a=armor();manager.innerHTML=`<div class="equipment-head"><div><span>LIVE EQUIPMENT</span><strong>Weapons, Gear & Consumables</strong><small data-equipment-sync-label>${isPreview()?'GM preview · local test · not synced':'Equipment state syncs with this character'}</small></div>${isPreview()?'<button type="button" data-reset-equipment>Reset equipment test</button>':''}</div><div class="equipment-grid"><section class="equip-section"><div class="equip-section-title"><span>EQUIPPED</span><strong>Ready now</strong></div><div class="equip-list">${activeMarkup(state.activePrimary,'ACTIVE PRIMARY')}${activeMarkup(state.activeSecondary,'ACTIVE SECONDARY')}<div class="equip-item active"><span>ACTIVE ARMOR</span><strong>${esc(a?.name||'Armor')}</strong><p>${esc(a?.meta||'')}</p></div></div></section><section class="equip-section"><div class="equip-section-title"><span>PACK</span><strong>Inventory · ${state.inventoryWeapons.length}/2 weapon slots</strong></div><div class="equip-list">${inventory||'<div class="equipment-empty">No unequipped weapons.</div>'}${consumables}${gear}</div></section></div>${lastConsumableResult?`<div class="consumable-result">${esc(lastConsumableResult)}</div>`:''}${lastNotice?`<div class="equipment-notice" role="status">${esc(lastNotice)}</div>`:''}<div class="equipment-rule"><b>Weapon switching:</b> free while calm or preparing; under danger/pressure mark 1 Stress. Inventory holds at most two weapons.</div>`;manager.querySelectorAll('[data-equip]').forEach(b=>b.addEventListener('click',()=>openEquip(b.dataset.equip)));manager.querySelectorAll('[data-use-weapon]').forEach(b=>b.addEventListener('click',()=>openWeaponUse(b.dataset.useWeapon)));manager.querySelectorAll('[data-remove-weapon]').forEach(b=>b.addEventListener('click',()=>removeWeapon(b.dataset.removeWeapon)));manager.querySelectorAll('[data-consume]').forEach(b=>b.addEventListener('click',()=>useConsumable(b.dataset.consume)));manager.querySelectorAll('[data-consume-adjust]').forEach(b=>b.addEventListener('click',()=>adjustConsumable(b.dataset.consumeAdjust,Number(b.dataset.delta))));manager.querySelector('[data-reset-equipment]')?.addEventListener('click',resetPreview);}
  function renderReady(){if(!state)return;const panel=ensureReady();if(!panel)return;const primary=weapon(state.activePrimary)?.name||'—',secondary=weapon(state.activeSecondary)?.name||'—';const consumableCount=Object.values(state.consumables).reduce((a,b)=>a+Number(b||0),0);const beast=activeKey==='marek'&&document.body.classList.contains('marek-beastform-active');panel.innerHTML=`<div class="ready-gear-copy"><span>READY GEAR</span><strong>${beast?'Beastform · humanoid weapons unavailable':`${esc(primary)}${secondary!=='—'?` + ${esc(secondary)}`:''}`}</strong><small>${esc(armor()?.name||'Armor')} · ${state.inventoryWeapons.length}/2 inventory weapons · ${consumableCount} consumable${consumableCount===1?'':'s'}</small></div><button type="button" data-open-gear>Open Gear</button>`;panel.querySelector('[data-open-gear]')?.addEventListener('click',openGearTab);}
  function openGearTab(){document.querySelector('[data-sheet-tab="gear"]')?.click();setTimeout(()=>document.getElementById('equipmentManager')?.scrollIntoView({behavior:'smooth',block:'start'}),80);}
  function activeWeaponIds(){return new Set([state?.activePrimary,state?.activeSecondary].filter(Boolean));}
  function cardTitle(node){return node.querySelector('.active-action-copy strong')?.textContent.trim()||node.querySelector('strong')?.textContent.trim()||'';}
  function applyActionAvailability(){if(!state)return;const active=activeWeaponIds(),offActions=new Set();state.ownedWeapons.forEach(id=>{const w=weapon(id);if(w&&!active.has(id))(w.actions||[]).forEach(action=>offActions.add(action));});document.querySelectorAll('#activeActionsPanel .active-action-card,#companionActionsPanel .active-action-card').forEach(card=>{const off=offActions.has(cardTitle(card));card.classList.toggle('equipment-action-disabled',off);if(off){card.disabled=true;card.dataset.equipmentDisabled='true';card.title='Weapon is not equipped.';}else if(card.dataset.equipmentDisabled==='true'){card.disabled=false;delete card.dataset.equipmentDisabled;card.removeAttribute('title');}});}
  function applyArmorScore(){if(!state)return;const stat=[...document.querySelectorAll('#characterSheet .character-stat')].find(n=>n.querySelector('span')?.textContent.trim().toLowerCase()==='armor');const strong=stat?.querySelector('strong');if(!strong)return;const base=armor()?.score||3,bonus=state.activeSecondary==='round-shield'?1:0,target=document.body.classList.contains('marek-beastform-active')&&activeKey==='marek'?3:base+bonus;if(Number(String(strong.textContent).match(/\d+/)?.[0])!==target)strong.textContent=String(target);}
  function apply(){if(!state)return;applyActionAvailability();applyArmorScore();renderReady();}
  function render(){if(!state)return;renderManager();renderReady();applyActionAvailability();applyArmorScore();window.GreywakeCharacterLayout?.normalize?.();window.GreywakeBackpack?.render?.();}
  function resetPreview(){if(!isPreview())return;state=normalise(cfg().base);lastConsumableResult='';lastNotice='Equipment test reset.';save();render();emit('Reset equipment test');}
  function init(){const k=key();if(!k){activeKey=null;activeStore=null;state=null;document.getElementById('readyGearPanel')?.remove();document.getElementById('equipmentManager')?.remove();return;}load(k);if(!resourceAPI()||!document.querySelector('#characterSheet .character-sheet-shell')){setTimeout(init,120);return;}render();}
  window.GreywakeEquipment={get key(){return activeKey;},getState:snapshot,importState,render,equip,openEquip,addWeapon,removeWeapon,adjustConsumable,useConsumable,openWeaponUse,openGear:openGearTab,isEquipped:id=>Boolean(state&&(state.activePrimary===id||state.activeSecondary===id)),isOwned:id=>Boolean(state?.ownedWeapons?.includes(id)),catalog:availableCatalog,weapon:id=>weapon(id),consumables:()=>clone(CONSUMABLES)};
  let timer;const schedule=()=>{clearTimeout(timer);timer=setTimeout(init,180);};window.addEventListener('greywake:player-ready',schedule);window.addEventListener('greywake:sheet-enhanced',schedule);window.addEventListener('hashchange',schedule);window.addEventListener('greywake:resources-changed',()=>setTimeout(()=>{apply();renderManager();},0));window.addEventListener('greywake:companion-resources-changed',()=>setTimeout(()=>{apply();renderManager();},0));window.addEventListener('greywake:damage-changed',()=>setTimeout(apply,0));window.addEventListener('greywake:rest-state-changed',()=>setTimeout(apply,0));document.addEventListener('click',event=>{if(event.target.closest?.('#chooseBeastform,#changeBeastform,#dropBeastform,[data-beastform]'))setTimeout(apply,120);});document.addEventListener('DOMContentLoaded',schedule);
})();