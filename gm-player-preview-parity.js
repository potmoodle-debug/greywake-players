(() => {
  // Retired. GM previews now boot as a fresh player-mode page in player-access.js.
  // Keeping this file as a no-op avoids stale HTML references reintroducing the old
  // in-place repair behaviour while cached pages transition to the new lifecycle.
})();
